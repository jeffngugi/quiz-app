import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const Main = ({children}) => {
  return (
    <View>
      {children}
    </View>
  )
}

export default Main

const styles = StyleSheet.create({})